import jdk.nashorn.internal.objects.Global.print

/*
fun main() {
    fun factorial(i:Int):Int{
            if (i==1||i==0) { return 1;
            }else{ return i*(factorial(i-1)); }
        }

        print(factorial(5))


    fun linerSearsh(array: Array<Int>,n:Int):Int{ for (i:Int in array){ if(array[i]==n) return i }
        return -1 }

    fun binarySearchAlgorithm(sortedArray:Array<Int>,key:Int):Int {
        var low:Int=0
        var high:Int=sortedArray.size
        var indext :Int = 0
        while (low < high) {
            var mid:Int = low  + ((high - low) / 2)
            if (sortedArray[mid] == key) { indext = mid;break }
            else if (sortedArray[mid] > key) {high = mid - 1; }
            else if (sortedArray[mid] < key) {low = mid + 1}
        };return indext
    }

}


*/